apiVersion: v1
kind: Service
metadata:
  name: owncloudsvc
  labels:
    app: svc
spec:
#   type: LoadBalancer
  type: NodePort  
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30180
    protocol: TCP
  selector:
    app: owncloud
